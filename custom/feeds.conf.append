
# ---------------------------------------------------------------------------
# Tailscale VPN — appended by custom/feeds.conf.append after upstream sync / build
# Official package lives in packages feed (net/tailscale); no extra src-git needed.
# Optimize after feeds install: ./scripts/apply-tailscale-optimize.sh
# ---------------------------------------------------------------------------
