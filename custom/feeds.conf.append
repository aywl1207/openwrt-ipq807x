# Official Tailscale package lives in the openwrt packages feed (net/tailscale).
# No extra src-git is required (avoids feed conflicts / duplicate packages).
# After feeds install: ./custom/scripts/apply-go-optimize.sh
