# Local feed for fork packages (not package/custom materialize).
# prepare.sh rewrites path to absolute (OpenWrt src-link requires absolute).
# Official Tailscale remains in the packages feed.
src-link custom_feed custom/feed
