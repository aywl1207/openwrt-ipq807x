# AdGuard Home / quic-go UDP buffers — sized for IPQ807x ~1GB class.
# Upstream package uses 7.5MB each; that reserves ~15MB permanently.
# 2.5MB is enough for DoH/DoQ on a home gateway without starving NSS/Wi-Fi.
net.core.rmem_max = 2500000
net.core.wmem_max = 2500000
