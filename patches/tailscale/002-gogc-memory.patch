--- a/files/tailscale.init
+++ b/files/tailscale.init
@@ -29,9 +29,14 @@ start_service() {
 
   # Starting with v1.48.1 ENV variable is required to enable use of iptables / nftables.
   # Use nftables by default - can be changed to 'iptables' in tailscale config
-  procd_set_param env TS_DEBUG_FIREWALL_MODE="$fw_mode"
+  # GOGC=10: 更激进的 Go GC，降低 tailscaled 在路由器上的常驻 RAM
+  procd_set_param env TS_DEBUG_FIREWALL_MODE="$fw_mode" GOGC=10
 
   # Disable logging to log.tailscale.com
   procd_append_param env TS_NO_LOGS_NO_SUPPORT=true
+
+  # 再次显式追加，确保 procd 环境中始终带有 GOGC（兼容旧版 procd_set_param 只吃首个 env 的情况）
+  procd_append_param env GOGC=10
 
   # Set the port to listen on for incoming VPN packets.
   # Remote nodes will automatically be informed about the new port number,
